import ExperimantalWindow from "./components/experimantal-window";

export default async function NewsPage() {
  return (
    <section className="w-screen h-screen overflow-hidden flex items-center justify-center">
      <ExperimantalWindow />
    </section>
  );
}
